import SectionWrapper from "./SectionWrapper";

const MyBestProjects = () => {
  return (
    <SectionWrapper className="bg-darkGray">
      My Best Projects Section
    </SectionWrapper>
  );
};

export default MyBestProjects;
